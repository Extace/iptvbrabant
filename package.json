{
  "name": "iptv-brabant-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint": "eslint . --ext .js,.html",
    "format": "prettier --write \"**/*.{css,js,html,json,md}\"",
    "test": "jest",
    "hasura:console": "hasura console --endpoint %HASURA_GRAPHQL_ENDPOINT% --admin-secret %HASURA_GRAPHQL_ADMIN_SECRET% --save",
    "hasura:migrate:apply": "hasura migrate apply --endpoint %HASURA_GRAPHQL_ENDPOINT% --admin-secret %HASURA_GRAPHQL_ADMIN_SECRET% --all-databases && hasura metadata apply --endpoint %HASURA_GRAPHQL_ENDPOINT% --admin-secret %HASURA_GRAPHQL_ADMIN_SECRET%",
    "hasura:metadata:export": "hasura metadata export --endpoint %HASURA_GRAPHQL_ENDPOINT% --admin-secret %HASURA_GRAPHQL_ADMIN_SECRET%"
  },
  "devDependencies": {
    "eslint": "^8.50.0",
    "prettier": "^2.8.8",
    "jest": "^29.6.0"
  }
}
